---
title: "gitのブランチ"
date: "2021-09-11T16:27:10Z"
updateDate: ""
category: "git"
tags: "git"
description: "まだ書いていません"
emoji: "🙂"
topImage: ""
---

# はじめに、なぜそうなったのか？から学べばキャッチアップしやすい

## git branch とは

- プロジェクトの状態を枝分かれさせること
- 複数人でプロジェクトを進める際に、同時に作業することが目的

# 基礎基本を抑える

## git branch が使用される理由

- プロジェクトの履歴を分岐して記録していくため
- 分岐したプロジェクトの状態（ブランチ）は他のブランチの影響を受けない為、
  同じプロジェクト内で複数の変更を同時に進めるため
- プロジェクトの各箇所を並行作業していくため

## git branch の種類

- デフォルトブランチ

  - プロジェクトの核
  - 名前は'master' , 'main'

- カレントブランチ
  - デフォルトブランチから複数のブランチが作成できる
  - その中から必ずどれかのブランチを選択する必要がある
  - その選択されているブランチをカレントブランチと言う

## git branch のコマンド

| コマンド                   | 効果                                                           | コマンド実行場所         | 　予期されること                                                                                       |
| -------------------------- | -------------------------------------------------------------- | ------------------------ | ------------------------------------------------------------------------------------------------------ |
| %git branch                | ローカルリポジトリのブランチ一覧表示                           | 対象のレポジトリ上       | 　カレントブランチに＊が表示                                                                           |
| %git branch -a             | ローカルと紐ついているリモートリポジトリ両方のブランチ一覧表示 | 対象のレポジトリ上       | リモートブランチに remotes/origin が表示                                                               |
| %git branch branch_name    | ローカルブランチの作成                                         | 対象のローカルブランチ上 | すでに存在するブランチ名を指定した場合エラー                                                           |
| %git branch -d branch_name | ローカルブランチの削除                                         | 対象のローカルブランチ上 | 削除したいブランチにコミットがあり、さらにそのコミットがどのブランチにもマージされていない場合はエラー |
| %git branch -D branch_name | ローカルブランチの強制削除                                     | 対象のローカルブランチ上 | 削除するブランチ以外のブランチをカレントブランチにしている必要がある                                   |

# 自分が理解できたドキュメントが必ず存在する

- その使用したドキュメントを、以下項目に沿って、
- かつ自分が理解したまま自分の言葉で言語化してまとめきる

## 参考ドキュメント

- Git 基礎
  - https://backlog.com/ja/git-tutorial/intro/01/
- Git ブランチ戦略
  - https://qiita.com/trsn_si/items/cfecbf7dff20c64628ea

# 今回の目的　達成したい箇所 ユースケース

- ブランチについての基礎を習得する

# 問題　理解できていない箇所

- ブランチのきりかた
- ブランチ戦略
- 実践

# 解決策　対策手順の概要

- 基礎を自分の言葉でまとめる
- 戦略は基礎から外れるので、コピペでまとめる
- 場数を増やす為、自分のレポジトリで実践する

# 前提条件　あれば書く　基本情報や必須条件など

# 目的達成手順　順序立てた目的達成手順

# まとめ　ポジティブ意見

- 一人で開発するケースはほぼ皆無の為、チーム開発する為の必須ツール
- 基礎を理解できれば、チーム開発に容易に参加できるので学習するメリットは大きい

# 自分がわからないと意味がないので自分の言葉でアウトプットすること

---

## 構成２　内向けの場合

1. 記事構成（記事構成）
2. 目的（解決したい問題）結論から宣言する
3. 解決策（対策手順の概要）
4. ユースケースのイメージ（ストーリーこれ ② かも）
5. 説明と理由（手順を段階的に）（理由を書くとイメージがさらに UP する）（関連記事へのリンク＝自分用、関連記事への後記＝稼ぎ用）
6. 実習（コード挿入）
7. まとめ　まとめるならポジティブ　まとめないケースもあるマナブさん
8. 稼ぐ要素を必ず添付
9. 応用リンク（⑤ の説明の後記）

---

## ひとつひとつ理解するための大義、フレームワーク

1. 目的と issue 本当に達成すべきか
2. 問題と改善 どのような問題を解決してくれるか
3. 基本の使い方 基本的な使い方だけ書く
4. 応用の使い方 応用して使うにはどのような知識を持っておけば良いかだけ書く
   ※前提として 1,2 のみで大枠だけ書くだけ
   ※応用は改善時に学習しないと定着不可能のため

---

## 下記自分ルール　これだけくりかえし

1. 選択と集中 選択が出来たところで無意味　意味あるものに集中して有意味　（自問:issue）
2. 理解と説明 理解できたところで無意味　難しいことをカンタンに説明ができる事で有意味 (自己理解:自分の言葉)
3. 施行と改善 施行を続けたところで無意味　施行ありきの改善を続けることで有意味 (自己改善:アップデート)

---
